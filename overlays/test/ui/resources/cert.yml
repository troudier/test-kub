apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ui-cert
  namespace: default
spec:
  commonName: $DOMAIN
  secretName: teacoffee-cert
  dnsNames:
    - $DOMAIN
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer