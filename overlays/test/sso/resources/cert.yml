apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sso-cert
  namespace: default
spec:
  commonName: $DOMAIN
  secretName: sso-cert-test
  dnsNames:
    - $DOMAIN
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer