apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-cert
  namespace: default
spec:
  commonName: $DOMAIN
  secretName: api-cert
  dnsNames:
    - $DOMAIN
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer